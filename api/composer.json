{
    "name": "mut-taucher/api",
    "type": "project",
    "authors": [
        {
            "name": "Tobias Winkler",
            "email": "info@tobiaswinkler.berlin"
        }
    ],
    "config": {
        "platform-check": false
    },
    "require": {
        "phpmailer/phpmailer": "^7.0",
        "tecnickcom/tcpdf": "^6.11",
        "stripe/stripe-php": "^19.4",
        "paypal/paypal-server-sdk": "^2.2"
    },
    "require-dev": {
        "phpunit/phpunit": "^13.0"
    },
    "scripts": {
        "test": "phpunit",
        "lint": "find lib routes templates -name '*.php' -exec php -l {} \\;",
        "check": [
            "@lint",
            "@test"
        ]
    }
}
